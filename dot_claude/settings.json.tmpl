{{- /* Claude Code Settings */ -}}
{
  "alwaysThinkingEnabled": true,
  "mcpServers": {
    "fetch": {
      "command": "docker",
      "args": [
        "run",
        "-i",
        "--rm",
        "mcp/fetch"
      ]
    }
  }
}
